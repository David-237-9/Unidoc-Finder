# Test Server
This is a simple test server that can be used to test the functionality of the client. It listens on port 8080 and responds to GET requests

# Run
To run the server, use the following command:

```bash
node server.mjs
```
